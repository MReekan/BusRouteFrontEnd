<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.busroute">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CurrentRoutePlaceActivity"></activity>
        <activity android:name=".CurrentRouteActivity" />
        <activity android:name=".RouteWithPlaceActivity" />
        <activity android:name=".BigilActivity" />
        <activity android:name=".ShowRouteListActivity" />
        <activity android:name=".NeedHelpActivity" />
        <activity android:name=".CurrentToDestinationActivity" />
        <activity android:name=".FindTheRouteActivity" />
        <activity android:name=".StartToDestinationActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDQ2UO6exL5KM4kVa7AjiXUsTlrTqfJzRo" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>